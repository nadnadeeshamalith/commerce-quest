import { grade6MathsPaper21Bank } from './ganithaya_paper21.js';
import { grade6MathsPaper22Bank } from './ganithaya_paper22.js';
import { grade6MathsPaper23Bank } from './ganithaya_paper23.js';
import { grade6MathsPaper24Bank } from './ganithaya_paper24.js';
import { grade6MathsPaper25Bank } from './ganithaya_paper25.js';
import { grade6MathsPaper26Bank } from './ganithaya_paper26.js';
import { grade6MathsPaper27Bank } from './ganithaya_paper27.js';
import { grade6MathsPaper28Bank } from './ganithaya_paper28.js';
import { grade6MathsPaper29Bank } from './ganithaya_paper29.js';
import { grade6MathsPaper30Bank } from './ganithaya_paper30.js';
import { grade6MathsPapers31to40Bank } from './ganithaya_papers_31_40.js';
import { grade6MathsPro31to40Bank } from './ganithaya_pro_31_40.js';

export const grade6MathsPaper1Bank = [
  {
    id: 61001,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "අංක 50 708 හි දශක ස්ථානයේ අගය කීයද?",
    options: ["0", "70", "700", "7 000"],
    answer: 0,
    marks: 1
  },
  {
    id: 61002,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "පහත අංක වලින් ලොකුම අංකය තෝරන්න.",
    options: ["36 405", "36 450", "36 054", "36 540"],
    answer: 3,
    marks: 1
  },
  {
    id: 61003,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "9 × 7 = ?",
    options: ["56", "63", "64", "72"],
    answer: 1,
    marks: 1
  },
  {
    id: 61004,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "1 248 + 562 = ?",
    options: ["1 700", "1 710", "1 810", "1 820"],
    answer: 2,
    marks: 1
  },
  {
    id: 61005,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "2 000 − 375 = ?",
    options: ["1 525", "1 625", "1 735", "1 875"],
    answer: 1,
    marks: 1
  },
  {
    id: 61006,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "84 ÷ 7 = ?",
    options: ["10", "11", "12", "14"],
    answer: 2,
    marks: 1
  },
  {
    id: 61007,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "පහත කුමන අංකය 3 න් බෙදෙනවාද?",
    options: ["1 246", "1 251", "1 254", "1 259"],
    answer: 0,
    marks: 1
  },
  {
    id: 61008,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "භාගය 6/18 සරලම ආකාරයට පත් කරන්නේ කෙසේද?",
    options: ["1/3", "2/3", "3/6", "6/9"],
    answer: 0,
    marks: 1
  },
  {
    id: 61009,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "1/2 සහ 3/4 අතරින් ලොකුම භාගය කුමක්ද?",
    options: ["1/2", "3/4", "සමානයි", "තීරණය කළ නොහැක"],
    answer: 1,
    marks: 1
  },
  {
    id: 61010,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "0.5 යනු පහත කුමන භාගයට සමානද?",
    options: ["1/5", "1/2", "2/5", "5/2"],
    answer: 1,
    marks: 1
  },
  {
    id: 61011,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "25% යනු පහත කුමන භාගයට සමානද?",
    options: ["1/2", "1/4", "3/4", "2/5"],
    answer: 1,
    marks: 1
  },
  {
    id: 61012,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "1 m = ? cm",
    options: ["10", "100", "1 000", "10 000"],
    answer: 1,
    marks: 1
  },
  {
    id: 61013,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "දිග 8 cm හා පළල 5 cm වන චතුරස්‍රාකාරයක පරිමිතිය කීයද?",
    options: ["13 cm", "26 cm", "40 cm", "16 cm"],
    answer: 1,
    marks: 1
  },
  {
    id: 61014,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "කෝණයක අංශ ප්‍රමාණය 90° නම් එය කුමන කෝණයක්ද?",
    options: ["තික්ෂ්ණ කෝණය", "සෘජු කෝණය", "විශාල කෝණය", "සම්පූර්ණ කෝණය"],
    answer: 1,
    marks: 1
  },
  {
    id: 61015,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "පහත රටාවේ ඊළඟ අංකය කුමක්ද? 4, 7, 10, 13, ...",
    options: ["15", "16", "17", "18"],
    answer: 2,
    marks: 1
  },
  {
    id: 61016,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "සමීකරණය x + 9 = 15 හි x අගය කීයද?",
    options: ["4", "5", "6", "7"],
    answer: 2,
    marks: 1
  },
  {
    id: 61017,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "කාලය 2:35 සිට 3:10 දක්වා ගතවන කාලය කීයද?",
    options: ["25 මිනිත්තු", "30 මිනිත්තු", "35 මිනිත්තු", "45 මිනිත්තු"],
    answer: 2,
    marks: 1
  },
  {
    id: 61018,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "72 හි සාධකයක් (factor) නොවන සංඛ්‍යාව තෝරන්න.",
    options: ["8", "9", "12", "14"],
    answer: 3,
    marks: 1
  },
  {
    id: 61019,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "15 සහ 20 හි LCM (අවම පොදු ගුණකය) කීයද?",
    options: ["30", "40", "60", "120"],
    answer: 2,
    marks: 1
  },
  {
    id: 61020,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "පහත අංකයන්ගෙන් ප්‍රථම සංඛ්‍යාව (prime) කුමක්ද?",
    options: ["21", "27", "29", "33"],
    answer: 2,
    marks: 1
  },
  {
    id: 61021,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "3/5 + 1/10 = ?",
    options: ["7/10", "1/2", "3/10", "2/5"],
    answer: 0,
    marks: 1
  },
  {
    id: 61022,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "2.75 − 0.9 = ?",
    options: ["1.85", "1.95", "2.05", "2.65"],
    answer: 0,
    marks: 1
  },
  {
    id: 61023,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "රු. 2 400 ක මිලකට 15% වට්ටමක් දුන් විට වට්ටමේ මුදල කීයද?",
    options: ["රු. 240", "රු. 300", "රු. 360", "රු. 400"],
    answer: 2,
    marks: 1
  },
  {
    id: 61024,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "දිග 12 cm, පළල 9 cm වන චතුරස්‍රාකාරයක පරිමාණය (area) කීයද?",
    options: ["21 cm²", "54 cm²", "108 cm²", "216 cm²"],
    answer: 2,
    marks: 1
  },
  {
    id: 61025,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "කූඩුවක මුළු ඇපල් 60ක් ඇත. එයින් 2/5 ක් විකුණා ඇත. විකුණා ඇති ඇපල් ගණන කීයද?",
    options: ["12", "20", "24", "40"],
    answer: 2,
    marks: 1
  },
  {
    id: 61026,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "1.2 m = ? cm",
    options: ["12", "120", "1 200", "0.12"],
    answer: 1,
    marks: 1
  },
  {
    id: 61027,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "සමීකරණය 3x = 24 හි x අගය කීයද?",
    options: ["6", "7", "8", "9"],
    answer: 2,
    marks: 1
  },
  {
    id: 61028,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "රටාව: 2, 6, 18, 54, ... ඊළඟ අංකය කුමක්ද?",
    options: ["72", "108", "162", "216"],
    answer: 2,
    marks: 1
  },
  {
    id: 61029,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "ඝනකයක් (cube) එකක එක් දාරයක දිග 4 cm නම් එහි පරිමාව (volume) කීයද?",
    options: ["16 cm³", "32 cm³", "48 cm³", "64 cm³"],
    answer: 3,
    marks: 1
  },
  {
    id: 61030,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "පහත තුනකොණ වලින් සමභුජ (equilateral) තුනකොණයක් කුමක්ද?",
    options: ["කෝණ 30°, 60°, 90°", "පැති 5 cm, 5 cm, 8 cm", "පැති 6 cm, 6 cm, 6 cm", "කෝණ 45°, 45°, 90°"],
    answer: 2,
    marks: 1
  },
  {
    id: 61031,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "පහත සමානතාවය නිවැරදිද? 0.25 = 25/100",
    options: ["ඔව්", "නැහැ", "ඒක 2.5 යි", "ඒක 0.0025 යි"],
    answer: 0,
    marks: 1
  },
  {
    id: 61032,
    subject: "G6M",
    paperId: 1,
    level: "medium",
    question: "කාලය 1h 45min = ? minutes",
    options: ["45", "60", "90", "105"],
    answer: 3,
    marks: 1
  },
  {
    id: 61033,
    subject: "G6M",
    paperId: 1,
    level: "hard",
    question: "එක් සිසුන් කණ්ඩායමක් විසින් රු. 3 600 ක මුදලක් සමානව සිසුන් 9 දෙනා අතර බෙදා ගනී. පසුව සිසුන් 3 දෙනාම ඉවත් වූ විට, ඉතිරි මුදල සමානව බෙදා ගත්තේ නම් එක් සිසුවෙකුට ලැබෙන මුදල කීයද?",
    options: ["රු. 400", "රු. 450", "රු. 600", "රු. 900"],
    answer: 2,
    marks: 1
  },
  {
    id: 61034,
    subject: "G6M",
    paperId: 1,
    level: "hard",
    question: "x/3 + 4 = 10 නම් x අගය කීයද?",
    options: ["12", "14", "16", "18"],
    answer: 0,
    marks: 1
  },
  {
    id: 61035,
    subject: "G6M",
    paperId: 1,
    level: "hard",
    question: "එක් චතුරස්‍රාකාරයේ පරිමිතිය 54 cm වේ. දිග පළලට වඩා 9 cm වැඩි නම්, පළල කීයද?",
    options: ["9 cm", "12 cm", "15 cm", "18 cm"],
    answer: 1,
    marks: 1
  },
  {
    id: 61036,
    subject: "G6M",
    paperId: 1,
    level: "hard",
    question: "1/2 + 2/3 + 3/4 = ?",
    options: ["23/12", "13/12", "17/12", "19/12"],
    answer: 2,
    marks: 1
  },
  {
    id: 61037,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "0.8 යනු පහත කුමන ප්‍රතිශතයට සමානද?",
    options: ["8%", "80%", "0.8%", "800%"],
    answer: 1,
    marks: 1
  },
  {
    id: 61038,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "පහත අංකය නව දහස් (nearest thousand) කරන්නේ කෙසේද? 6 520",
    options: ["6 000", "6 500", "7 000", "7 500"],
    answer: 2,
    marks: 1
  },
  {
    id: 61039,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "භාගය 3/8 දශමයකට පරිවර්තනය කළ විට ලැබෙන්නේ කුමක්ද?",
    options: ["0.375", "0.38", "0.35", "0.625"],
    answer: 0,
    marks: 1
  },
  {
    id: 61040,
    subject: "G6M",
    paperId: 1,
    level: "easy",
    question: "පහත කුමන අංක දෙක එකතුව 1 දක්වා (exactly 1) වේද?",
    options: ["0.2 සහ 0.7", "0.3 සහ 0.7", "0.6 සහ 0.5", "0.4 සහ 0.7"],
    answer: 1,
    marks: 1
  }
];

const formatInt = (n) => String(n).replace(/\B(?=(\d{3})+(?!\d))/g, ' ');
const gcd = (a, b) => {
  let x = Math.abs(Number(a));
  let y = Math.abs(Number(b));
  while (y) {
    const t = y;
    y = x % y;
    x = t;
  }
  return x || 1;
};

const makeMCQ = ({ id, paperId, level, question, correct, distractors, marks = 1, explanation }) => {
  const options = [correct, ...distractors].map(String).slice(0, 4);
  const uniq = [];
  for (const o of options) {
    if (!uniq.includes(o)) uniq.push(o);
  }
  while (uniq.length < 4) uniq.push(String(Number(uniq[uniq.length - 1]) + 1));
  const answer = uniq.indexOf(String(correct));
  return {
    id,
    subject: "G6M",
    paperId,
    level,
    question,
    options: uniq,
    answer: answer === -1 ? 0 : answer,
    marks,
    explanation: explanation || ""
  };
};

const genPaper = (paperId) => {
  const out = [];
  for (let i = 1; i <= 40; i++) {
    const id = 62000 + (paperId * 100) + i;
    const topic = (i - 1) % 10;
    const lvl = i <= 24 ? "easy" : (i <= 36 ? "medium" : "hard");

    if (topic === 0) {
      const n = paperId * 10000 + 5000 + i * 37;
      const tens = Math.floor((n % 100) / 10) * 10;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `අංක ${formatInt(n)} හි දශක ස්ථානයේ අගය කීයද?`,
        correct: tens,
        distractors: [Math.floor(n % 10), Math.floor((n % 1000) / 100) * 100, Math.floor(n / 1000) * 1000]
      }));
      continue;
    }

    if (topic === 1) {
      const a = paperId * 900 + i * 17 + 320;
      const b = paperId * 700 + i * 11 + 280;
      const ans = a + b;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `${formatInt(a)} + ${formatInt(b)} = ?`,
        correct: formatInt(ans),
        distractors: [formatInt(ans - 10), formatInt(ans + 10), formatInt(ans - 100)]
      }));
      continue;
    }

    if (topic === 2) {
      const a = paperId * 1100 + i * 29 + 900;
      const b = paperId * 600 + i * 13 + 150;
      const ans = a - b;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `${formatInt(a)} − ${formatInt(b)} = ?`,
        correct: formatInt(ans),
        distractors: [formatInt(ans + 10), formatInt(ans - 10), formatInt(ans + 100)]
      }));
      continue;
    }

    if (topic === 3) {
      const x = (paperId % 7) + 6;
      const y = (i % 8) + 3;
      const ans = x * y;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `${x} × ${y} = ?`,
        correct: ans,
        distractors: [ans + x, ans - y, ans + y]
      }));
      continue;
    }

    if (topic === 4) {
      const base = (paperId % 5) + 2;
      const num = base;
      const den = (i % 7) + 8;
      const g = gcd(num, den);
      const sn = num / g;
      const sd = den / g;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `භාගය ${num}/${den} සරලම ආකාරයට පත් කරන්නේ කුමක්ද?`,
        correct: `${sn}/${sd}`,
        distractors: [`${num}/${den}`, `${sn}/${sd + 1}`, `${sn + 1}/${sd}`]
      }));
      continue;
    }

    if (topic === 5) {
      const a = (paperId % 4) + 1;
      // For this topic, i values repeat in steps of 10 within the same paper,
      // so (i % 5) becomes constant and duplicates happen. Use an occurrence index instead.
      const occ5 = Math.floor((i - 6) / 10); // i is 6,16,26,36
      const b = (occ5 % 5) + 2;
      const frac = `${a}/${b}`;
      const pct = Math.round((a / b) * 100);
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `භාගය ${frac} ප්‍රතිශතයක් ලෙස (ආසන්නතම %) ලියන විට ලැබෙන්නේ කුමක්ද?`,
        correct: `${pct}%`,
        distractors: [`${pct + 5}%`, `${pct - 5}%`, `${pct * 2}%`]
      }));
      continue;
    }

    if (topic === 6) {
      const cm = (paperId * 40) + i * 3 + 20;
      const m = cm / 100;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `${cm} cm = ? m`,
        correct: m.toFixed(2),
        distractors: [(m * 10).toFixed(2), (m / 10).toFixed(2), (m + 1).toFixed(2)]
      }));
      continue;
    }

    if (topic === 7) {
      const l = (paperId % 6) + 6 + (i % 4);
      const w = (paperId % 5) + 4 + (i % 3);
      const per = 2 * (l + w);
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `දිග ${l} cm හා පළල ${w} cm වන චතුරස්‍රාකාරයක පරිමිතිය කීයද?`,
        correct: `${per} cm`,
        distractors: [`${l + w} cm`, `${l * w} cm`, `${per + 2} cm`]
      }));
      continue;
    }

    if (topic === 8) {
      const h = (paperId % 4) + 1;
      const m1 = (i * 5) % 60;
      const m2 = (m1 + 25 + paperId) % 60;
      const dur = (m2 - m1 + 60) % 60;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `කාලය ${h}:${String(m1).padStart(2, '0')} සිට ${h}:${String(m2).padStart(2, '0')} දක්වා ගතවන කාලය කීයද?`,
        correct: `${dur} මිනිත්තු`,
        distractors: [`${dur + 5} මිනිත්තු`, `${Math.max(0, dur - 5)} මිනිත්තු`, `${60 - dur} මිනිත්තු`]
      }));
      continue;
    }

    const x = (paperId % 7) + 4;
    // For the final "topic 9" block, i values repeat in steps of 10,
    // so (i % 5) becomes constant and duplicates happen. Use an occurrence index instead.
    const occ9 = Math.floor((i - 10) / 10); // i is 10,20,30,40
    const rhs = x + 9 + occ9;
    out.push(makeMCQ({
      id,
      paperId,
      level: lvl,
      question: `සමීකරණය x + ${9 + occ9} = ${rhs} හි x අගය කීයද?`,
      correct: x,
      distractors: [x + 1, x - 1, x + 2]
    }));
  }
  return out;
};

const genPaper11to20 = (paperId) => {
  const out = [];
  for (let i = 1; i <= 40; i++) {
    const id = 62000 + (paperId * 100) + i;
    const topic = (i - 1) % 10;
    const lvl = i <= 24 ? "easy" : (i <= 36 ? "medium" : "hard");

    if (topic === 0) {
      const n = paperId * 10000 + 2000 + i * 41;
      const place = (i % 3 === 0) ? "සියක" : ((i % 3 === 1) ? "දශක" : "එකක");
      const value = place === "සියක"
        ? Math.floor((n % 1000) / 100) * 100
        : (place === "දශක" ? Math.floor((n % 100) / 10) * 10 : (n % 10));
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `අංක ${formatInt(n)} හි ${place} ස්ථානයේ අගය කීයද?`,
        correct: value,
        distractors: [n % 10, Math.floor((n % 100) / 10) * 10, Math.floor((n % 1000) / 100) * 100],
        explanation: `අරමුණ: ස්ථාන අගය හඳුනාගැනීම (සංඛ්‍යා පද්ධති).`
      }));
      continue;
    }

    if (topic === 1) {
      const a = paperId * 800 + i * 19 + 450;
      const b = paperId * 600 + i * 13 + 280;
      const op = i % 2 === 0 ? "+" : "−";
      const ans = op === "+" ? (a + b) : (a - b);
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `${formatInt(a)} ${op} ${formatInt(b)} = ?`,
        correct: formatInt(ans),
        distractors: [formatInt(ans + 10), formatInt(ans - 10), formatInt(ans + 100)],
        explanation: `අරමුණ: හතරමුල්‍ය ක්‍රියා (එකතු/අඩු කිරීම) නිවැරදිව කිරීම.`
      }));
      continue;
    }

    if (topic === 2) {
      const x = (paperId % 9) + 4;
      const y = (i % 11) + 2;
      const isDiv = i % 3 === 0;
      if (!isDiv) {
        const ans = x * y;
        out.push(makeMCQ({
          id,
          paperId,
          level: lvl,
          question: `${x} × ${y} = ?`,
          correct: ans,
          distractors: [ans + x, ans + y, Math.max(0, ans - x)],
          explanation: `අරමුණ: ගුණ කිරීමේ නිවැරදි ගණනය.`
        }));
      } else {
        const dividend = x * y;
        out.push(makeMCQ({
          id,
          paperId,
          level: lvl,
          question: `${dividend} ÷ ${x} = ?`,
          correct: y,
          distractors: [y + 1, y - 1, y + 2],
          explanation: `අරමුණ: බෙදීම (division) ගණනය කිරීම.`
        }));
      }
      continue;
    }

    if (topic === 3) {
      const den = (i % 8) + 5;
      const a = (paperId % 3) + 1;
      const b = (i % 3) + 1;
      const n1 = Math.min(den - 1, a + 1);
      const n2 = Math.min(den - 1, b + 2);
      const sum = n1 + n2;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `${n1}/${den} + ${n2}/${den} = ?`,
        correct: `${sum}/${den}`,
        distractors: [`${sum}/${den + 1}`, `${sum - 1}/${den}`, `${sum + 1}/${den}`],
        explanation: `අරමුණ: එකම මූලය (denominator) ඇති භාග එකතු කිරීම.`
      }));
      continue;
    }

    if (topic === 4) {
      const whole = (paperId % 4) + 1;
      // For this topic, i values repeat in steps of 10 within the same paper,
      // so (i * 3) % 10 becomes constant and duplicates happen.
      const occ4 = Math.floor((i - 5) / 10); // i is 5,15,25,35
      const baseDigit = paperId % 10;
      const d1 = (baseDigit + occ4) % 10;
      const d2 = (baseDigit + occ4 + 3) % 10;
      const dec1 = Number(`${whole}.${d1}`);
      const dec2 = Number(`${whole}.${d2}`);
      const correct = dec1 > dec2 ? dec1.toFixed(1) : dec2.toFixed(1);
      const smaller = dec1 > dec2 ? dec2.toFixed(1) : dec1.toFixed(1);
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `පහත දශම අංක දෙකෙන් ලොකුම එක තෝරන්න: ${dec1.toFixed(1)} සහ ${dec2.toFixed(1)}`,
        correct,
        distractors: [smaller, (Number(correct) + 0.1).toFixed(1), (Number(correct) - 0.1).toFixed(1)],
        explanation: `අරමුණ: දශම අංක සංසන්දනය කිරීම (දශම).`
      }));
      continue;
    }

    if (topic === 5) {
      const base = paperId * 60 + i * 8 + 120;
      const pct = [10, 15, 20, 25, 30][i % 5];
      const ans = Math.round((pct / 100) * base);
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `${base} හි ${pct}% කීයද?`,
        correct: ans,
        distractors: [Math.round(((pct + 5) / 100) * base), Math.round(((pct - 5) / 100) * base), Math.round((pct / 10) * base)],
        explanation: `අරමුණ: ප්‍රතිශතය භාවිතා කර සංඛ්‍යාගණනය කිරීම (ප්‍රතිශත).`
      }));
      continue;
    }

    if (topic === 6) {
      const mode = i % 3;
      if (mode === 0) {
        const cm = paperId * 25 + i * 4 + 36;
        out.push(makeMCQ({
          id,
          paperId,
          level: lvl,
          question: `${cm} cm = ? m`,
          correct: (cm / 100).toFixed(2),
          distractors: [(cm / 10).toFixed(2), (cm / 1000).toFixed(3), ((cm / 100) + 1).toFixed(2)],
          explanation: `අරමුණ: දිග ඒකක පරිවර්තනය (cm → m) (මිනුම්).`
        }));
      } else if (mode === 1) {
        const kg = (paperId % 5) + 2;
        const g = i * 75 + 125;
        out.push(makeMCQ({
          id,
          paperId,
          level: lvl,
          question: `${kg} kg ${g} g = ? g`,
          correct: kg * 1000 + g,
          distractors: [kg * 100 + g, kg * 1000 + g + 100, kg * 1000 + g - 100],
          explanation: `අරමුණ: භාර ඒකක පරිවර්තනය (kg/g) (මිනුම්).`
        }));
      } else {
        const l = (paperId % 4) + 1;
        const ml = i * 120 + 250;
        out.push(makeMCQ({
          id,
          paperId,
          level: lvl,
          question: `${l} L ${ml} mL = ? mL`,
          correct: l * 1000 + ml,
          distractors: [l * 100 + ml, l * 1000 + ml + 50, l * 1000 + ml - 50],
          explanation: `අරමුණ: ධාරිතාව ඒකක පරිවර්තනය (L/mL) (මිනුම්).`
        }));
      }
      continue;
    }

    if (topic === 7) {
      const pick = i % 3;
      if (pick === 0) {
        const l = (paperId % 7) + 5;
        const w = (paperId % 5) + 3 + (i % 3);
        const area = l * w;
        out.push(makeMCQ({
          id,
          paperId,
          level: lvl,
          question: `දිග ${l} cm හා පළල ${w} cm වන චතුරස්‍රාකාරයක පරිමාණය (Area) කීයද?`,
          correct: `${area} cm²`,
          distractors: [`${2 * (l + w)} cm`, `${l + w} cm²`, `${area + l} cm²`],
          explanation: `අරමුණ: චතුරස්‍රාකාර පරිමාණය ගණනය කිරීම (ජ්‍යාමිතිය).`
        }));
      } else if (pick === 1) {
        const angle = [45, 90, 120, 135, 180][(paperId + i) % 5];
        const kind = angle === 90 ? "සෘජු කෝණය" : (angle < 90 ? "තික්ෂ්ණ කෝණය" : (angle < 180 ? "විශාල කෝණය" : "සෘජු රේඛා කෝණය"));
        out.push(makeMCQ({
          id,
          paperId,
          level: lvl,
          question: `${angle}° යනු කුමන වර්ගයේ කෝණයක්ද?`,
          correct: kind,
          distractors: ["තික්ෂ්ණ කෝණය", "සෘජු කෝණය", "විශාල කෝණය"].filter(x => x !== kind).slice(0, 3),
          explanation: `අරමුණ: කෝණ වර්ග හඳුනාගැනීම (ජ්‍යාමිතිය).`
        }));
      } else {
        // For this specific branch (pick === 2), i values that hit this branch within
        // a single paper repeat in a way that makes `s` constant -> duplicate question text.
        const occ7 = Math.floor((i - 8) / 10); // i is 8,18,28,38 for topic 7
        const s = (paperId % 5) + 4 + occ7;
        out.push(makeMCQ({
          id,
          paperId,
          level: lvl,
          question: `පැති ${s} cm, ${s} cm, ${s} cm වන තුනකොණය කුමන වර්ගයේද?`,
          correct: "සමභුජ තුනකොණය",
          distractors: ["සමදෙපැති තුනකොණය", "විෂමපැති තුනකොණය", "සෘජු තුනකොණය"],
          explanation: `අරමුණ: තුනකොණ වර්ග හඳුනාගැනීම (ජ්‍යාමිතිය).`
        }));
      }
      continue;
    }

    if (topic === 8) {
      const a = (paperId % 4) + 2;
      // This topic repeats in steps of 10 within a paper, so (i % 5) becomes constant.
      // Use occurrence index to vary values and avoid duplicates.
      const occ8 = Math.floor((i - 9) / 10); // i is 9,19,29,39
      const b = (occ8 % 5) + 2;
      const c = (paperId % 3) + 2;
      const d = (occ8 % 4) + 1;
      const data = [a, b, c, d, a];
      const mode = a;
      const sum = data.reduce((s, v) => s + v, 0);
      const mean = (sum / data.length).toFixed(1);
      const askMean = i % 2 === 0;
      out.push(makeMCQ({
        id,
        paperId,
        level: lvl,
        question: `දත්ත කට්ටලය: ${data.join(', ')}. ${askMean ? "සාමාන්‍යය (Mean) කීයද?" : "බහුලය (Mode) කුමක්ද?"}`,
        correct: askMean ? mean : mode,
        distractors: askMean ? [(Number(mean) + 0.2).toFixed(1), (Number(mean) - 0.2).toFixed(1), String(sum)] : [mode + 1, mode - 1, b],
        explanation: `අරමුණ: දත්ත විශ්ලේෂණයේ (mean/mode) මූලික සංකල්ප භාවිතය.`
      }));
      continue;
    }

    const start = (paperId % 6) + 2;
    // Topic 9 i values repeat in steps of 10, causing i % 4 to toggle and duplicates.
    const occ9 = Math.floor((i - 10) / 10); // i is 10,20,30,40
    const diff = (occ9 % 4) + 3;
    const term = start + diff * 6;
    out.push(makeMCQ({
      id,
      paperId,
      level: lvl,
      question: `රටාව: ${start}, ${start + diff}, ${start + 2 * diff}, ... මෙහි 7 වන පදය කුමක්ද?`,
      correct: term,
      distractors: [term + diff, term - diff, term + 2 * diff],
      explanation: `අරමුණ: සංඛ්‍යා රටා (arithmetic pattern) හඳුනාගෙන පද සොයීම.`
    }));
  }
  return out;
};

export const grade6MathsBank = [
  ...(grade6MathsPaper1Bank || []),
  ...genPaper(2),
  ...genPaper(3),
  ...genPaper(4),
  ...genPaper(5),
  ...genPaper(6),
  ...genPaper(7),
  ...genPaper(8),
  ...genPaper(9),
  ...genPaper(10),
  ...genPaper11to20(11),
  ...genPaper11to20(12),
  ...genPaper11to20(13),
  ...genPaper11to20(14),
  ...genPaper11to20(15),
  ...genPaper11to20(16),
  ...genPaper11to20(17),
  ...genPaper11to20(18),
  ...genPaper11to20(19),
  ...genPaper11to20(20),
  ...(typeof grade6MathsPaper21Bank !== 'undefined' ? grade6MathsPaper21Bank : []),
  ...(typeof grade6MathsPaper22Bank !== 'undefined' ? grade6MathsPaper22Bank : []),
  ...(typeof grade6MathsPaper23Bank !== 'undefined' ? grade6MathsPaper23Bank : []),
  ...(typeof grade6MathsPaper24Bank !== 'undefined' ? grade6MathsPaper24Bank : []),
  ...(typeof grade6MathsPaper25Bank !== 'undefined' ? grade6MathsPaper25Bank : []),
  ...(typeof grade6MathsPaper26Bank !== 'undefined' ? grade6MathsPaper26Bank : []),
  ...(typeof grade6MathsPaper27Bank !== 'undefined' ? grade6MathsPaper27Bank : []),
  ...(typeof grade6MathsPaper28Bank !== 'undefined' ? grade6MathsPaper28Bank : []),
  ...(typeof grade6MathsPaper29Bank !== 'undefined' ? grade6MathsPaper29Bank : []),
  ...(typeof grade6MathsPaper30Bank !== 'undefined' ? grade6MathsPaper30Bank : []),
  ...(typeof grade6MathsPapers31to40Bank !== 'undefined' ? grade6MathsPapers31to40Bank : []),
  ...(typeof grade6MathsPro31to40Bank !== 'undefined' ? grade6MathsPro31to40Bank : [])
];
